import {
  Meta,
  Title,
  Description,
  Primary,
  ArgTypes,
  Canvas,
} from '@storybook/addon-docs/blocks';

import * as SimpleStories from './simple.stories';
import * as StyledStories from './styled-scrollable.stories';
import * as LazyLoadingStories from './lazy-loading-scrollable.stories';

<Meta title="Docs" />

<Title>Docs</Title>

<Description of={SimpleStories} />

<Primary of={SimpleStories} />

## Supported properties:

Accepts all `HTML` element properties for the inner `overflow` element, plus additional properties listed in the table below:

<ArgTypes of={SimpleStories} />

## Examples

<Canvas of={SimpleStories.HorizontallyAndVerticallyScrollable} />
<Canvas of={SimpleStories.HorizontallyScrollable} />
<Canvas of={SimpleStories.VerticallyScrollable} />
<Canvas of={SimpleStories.NotScrollable} />
<Canvas of={StyledStories.CustomScrollbarsVariant1} />
<Canvas of={StyledStories.CustomScrollbarsVariant2} />
<Canvas of={StyledStories.CustomScrollbarsVariant3} />
<Canvas of={StyledStories.CustomScrollbarsVariant4} />
<Canvas of={LazyLoadingStories.LazyHorizontalScrollable} />
<Canvas of={LazyLoadingStories.LazyVerticalScrollable} />
